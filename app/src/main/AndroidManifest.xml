<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hardwork">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.PersonActivity"></activity>
        <activity android:name=".ui.Login" />
        <activity android:name=".ui.ViewVideo" />
        <activity
            android:name=".ui.ExerciseActivity"
            android:theme="@style/AppThemeNoTitle" />
        <activity
            android:name=".ui.TestActivity"
            android:theme="@style/AppThemeNoTitle" />
        <activity
            android:name=".ui.WelcomActivity"
            android:theme="@style/AppThemeNoTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.StudyView" />
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>